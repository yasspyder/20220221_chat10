import { makeStyles } from "@mui/styles";

// jss
export const useStyles = makeStyles((ctx) => {
  return {
    input: {
      color: "#9a9fa1",
      padding: "10px 15px",
      fontSize: "15px",
    },
    icon: {
      color: "#2b5278",
    },
  };
});
